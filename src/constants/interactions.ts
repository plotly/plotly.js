/**
 * Timing information for interactive elements
 */
export const SHOW_PLACEHOLDER = 100;
export const HIDE_PLACEHOLDER = 1000;

// Opacity dimming fraction for points that are not in selection
export const DESELECTDIM = 0.2;
